import { TestBed } from '@angular/core/testing';
import { InvitationProviderService } from './invitation-provider.service';
describe('InvitationProviderService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(InvitationProviderService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=invitation-provider.service.spec.js.map